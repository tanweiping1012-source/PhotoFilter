# 旅行照片筛选器项目规则

- 原图目录一律视为只读；禁止实现默认删除、移动、覆盖或改名原图的行为。
- 导出仅允许复制，并且必须在界面中展示目标目录和待复制数量后再确认。
- 不将原图、缩略图缓存、API Key、个人绝对路径或真实旅行照片提交到 Git、日志或公开测试数据。
- AI评分接入前必须定义并校验 JSON schema、photo_id 对齐和数值范围；非法结果不能参与排序。
- 每次改动后运行 `scripts/check.sh`。涉及扫描或导出逻辑时，补充相应测试，并手动验证源目录未被修改。
- 本地界面验证使用 `scripts/run-app.sh` 启动标准 `.app` 包，不依赖裸可执行文件。
- 每项任务必须有明确的可观察验收结果；完成时说明实际运行过的检查和结果。
- 优先完成一个可验收的垂直切片，避免在未验证前扩展功能范围。
- 同一窗口、同一状态下，同一个用户动作只能有一个可见主入口；统计信息不能伪装成重复按钮。键盘快捷键、右键菜单等替代操作可以保留，但必须共享同一命令和状态。改 UI 时同时清理已不再使用的旧视图代码。
- 选片核心流程必须支持“缩略图扫描 → 大图检查 → 决定 → 下一张”的连续操作；不能要求用户仅凭小缩略图判断清晰度、表情或构图。大图预览必须继续只读，不提供修改原图的入口。
- 已实现的业务能力必须存在可发现的可见入口；不得只留在 ViewModel、快捷键或右键菜单中。按钮禁用必须对应真实不可执行状态，能通过状态提示解释的问题应允许用户触发并获得反馈。
- 全局设置不能依赖已有照片项目才能访问；项目级状态和全局偏好应分开呈现。一个设置动作只能保留一个可见入口，避免侧栏和内容区重复。
- 密集工具栏只展示当前判断所需的短摘要；完整技术风险、本地排序和 AI 理由放入与当前照片绑定的详情视图，避免长文案挤压筛选与主操作。
- 待评分池容量是成本与覆盖上限，不是必须凑满的配额；不得为达到 `4N` 把低新颖度或同场景照片补入待评分池。同一视觉相似照片家族最多向 AI 输出一个代表，无法保证足够独立照片时应缩小待评分池或提示调整目标。
- 用户概念只使用“相似照片”，不得展示“连拍组”“相似组”或“候选组”。画面感知相似是建立家族的必要条件；拍摄时间只能为中等画面相似提高置信度，不能单独触发候选折叠、AI 分组或最终冲突。
- 旅行照片必须按用户概念分为“人物”和“风景”。“人物”表示人物承担主要视觉主题，不等于画面中检测到任意人体；清晰人像、大比例人物主体和显著人物风景/剪影可进入人物，远处路人和背景人群仍属风景。自动分类只允许在本机执行；用户必须能纠正分类，且人工纠正优先于后台分析并按相对路径持久化。
- 人物和风景必须拥有独立目标、待评分池、评分 scope、运行状态、全局排序和评分优先集合。不得跨类型比较分数、借用另一类型照片填补目标或生成跨类型总名次。
- 导出仍只保留一个主入口，但必须同时校验人物与风景目标，并在同一根目录中分别复制到人物和风景子目录；分类不得改变、移动或覆盖源照片。
- 上传图片给视觉模型判断的用户流程统一命名为“AI评分”，不得使用其他同义业务名称；筛选阶段固定为“待AI评分”“已AI评分”和“评分优先”。
- 首次正常启动展示版本化“第一次筛选”场景首页，完成或跳过后不重复弹出；侧栏“新手引导”入口必须可随时重开。完整体验必须从未评分照片开始，在真实网格中引导打开大图、人工保留、离线 AI评分、查看评分、采纳结果和导出；离线评分不读取 Keychain、不联网、不持久化，`--review-demo` 自动审核启动必须旁路首次弹窗并直接进入任务。
- AI 功能固定使用 BYOK：用户自行选择已实现的视觉模型并提供对应供应商 API Key。每个供应商的 Key 必须使用独立 Keychain 条目，不得进入 UserDefaults、项目状态、日志、诊断信息、审核样例或导出文件；App 内不得提供购买、充值或跳转购买 API Key 的入口。
- 模型目录只能展示已经实现协议适配器、确认支持图片输入且通过契约测试的模型；不得用不可点击占位项伪装支持。新增协议时必须同时更新供应商域名、隐私披露、错误处理、双语文案和自动门禁。
- AI 设置先选择品牌，再选择该品牌下支持图片输入的模型。内置品牌覆盖火山方舟、MiniMax、OpenAI、Anthropic、Google Gemini、阿里百炼、xAI、Kimi、智谱 GLM、腾讯混元与自定义 OpenAI-compatible。模型升级优先复用协议适配器，不得为同协议模型复制解析器。
- OpenAI 与 Anthropic 必须保留账号模型刷新和品牌内其他 model ID 入口；发现列表只提供候选，不能绕过正式图片连接验证。验证身份必须包含品牌和真实 API model ID。
- 新 API Key 必须用 1 张内置测试图走正式图片请求与评分契约验证，成功后才能写入 Keychain；失败不得保存新 Key、回显响应正文或读取用户照片。
- 高级设置允许配置一个自定义 OpenAI-compatible Chat Completions 模型；非回环地址必须使用 HTTPS，接口地址不得包含用户名、密码、query 或 fragment。自定义模型只承诺协议兼容，不得标记为内置已验证模型。
- 模型选择属于非敏感全局偏好，可持久化但不得写入照片项目。每次 AI评分必须在用户确认时锁定模型与供应商；设置变化不能改变已经确认或正在执行的批次。
- 用户可见的 AI评分进度只使用照片数量和照片范围，不得展示“批次”“第 N 批”或总批次数。内部仍可按 2–5 张组织请求，但进度条必须按已通过校验的照片数计算。
- 所有供应商适配器必须复用同一匿名 `photo_id`、用户确认的评分图片尺寸、无元数据 JPEG 和本地严格响应校验。评分图片尺寸固定为小 512px、中 1024px、大 1536px（最长边），默认小；不得发送原图。适配器只负责供应商请求/响应格式，不能绕过候选范围、名次、总分、五维评分、具体评价、总结或最终数量校验。
- 评分图片尺寸属于非敏感全局偏好，可持久化但不得写入照片项目。完整 AI评分必须在确认时锁定尺寸；设置变化不能改变已确认或正在执行的任务。设置页和发送确认必须同时展示档位、像素、上传量/延迟/潜在费用差异。
- 跨启动项目访问必须使用 security-scoped bookmark；持久化只保存最小项目元数据、相对路径人工决定、人物/风景目标和用户分类纠正，不保存自动分类、原图、缩略图、AI 上传预览、AI 原始响应或明文绝对路径。
- 持久化写入必须原子化并带 schemaVersion；权限失效时保留项目记录并明确要求重新授权，不得静默访问其他目录或自动扩大权限。
